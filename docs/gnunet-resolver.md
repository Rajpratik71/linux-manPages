GNUNET-RESOLVER(1)                     General Commands Manual                     GNUNET-RESOLVER(1)

NAME
       gnunet-resolver - build-in GNUnet stub resolver

SYNOPSIS
       gnunet-resolver [options][hostname]

DESCRIPTION
       gnunet-resolver uses build-in GNUnet stub resolver.

OPTIONS
       -c FILENAME,  --config=FILENAME
              Use the configuration file FILENAME.

       -h, --help
              Print short help on options.

       -L LOGLEVEL, --loglevel=LOGLEVEL
              Use LOGLEVEL for logging.  Valid values are DEBUG, INFO, WARNING and ERROR.

       -l LOGFILE, --logfile=LOGFILE
              Configure logging to write logs to LOGFILE.

       -r, --reverse
              Perform a reverse lookup.

       -v, --version
              Print GNUnet version number.

BUGS
       Report  bugs  by  using  Mantis  <https://gnunet.org/bugs/>  or  by sending electronic mail to
       <gnunet-developers@gnu.org>

GNUnet                                       Jan 4, 2012                           GNUNET-RESOLVER(1)
